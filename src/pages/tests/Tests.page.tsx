import { Stack } from '@mui/material';

const TestsPage = () => {
	//const { user } = useUser();
	//const router = useRouter();
	return (
		<div>
			<Stack
				sx={{
					diasplay: 'flex',
					height: '100px',
					width: '100%',
					alignItems: 'center',
					justifyContent: 'center'
				}}
			>
				tests
			</Stack>
		</div>
	);
};

export default TestsPage;
